# MongoDB Community Memory — Tasks

- [x] Add the official MongoDB Node.js driver.
- [x] Implement MongoDB and in-memory stores behind one interface.
- [x] Add indexes, idempotent seed data, CRUD, audit writes, and aggregation insights.
- [x] Add Atlas Search with a MongoDB text-index fallback.
- [x] Add a JSON API without exposing the connection string to the browser.
- [x] Add storage/search mode indicators and an event-memory search interface.
- [x] Add synthetic-only request validation.
- [x] Add deterministic store tests that require no external account.
- [x] Document the Atlas setup and workshop flow.
- [ ] Connect a personal Atlas free-tier cluster and capture a real run note.
- [ ] Publish the MongoDB-backed server and add the live URL.
